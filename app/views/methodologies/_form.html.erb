<%= form_for(methodology) do |f| %>
  <% if methodology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(methodology.errors.count, "error") %> prohibited this methodology from being saved:</h2>

      <ul>
      <% methodology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :methodology_id %>
    <%= f.number_field :methodology_id %>
  </div>

  <div class="field">
    <%= f.label :methodology_name %>
    <%= f.text_area :methodology_name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
